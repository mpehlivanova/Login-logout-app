export enum TokenType {
  accessToken = 'accessToken',
  idToken = 'idToken',
}

export enum Pages {
  home = 'home',
  favorite = 'favorite',
  user = 'user',
  pageNotFound = 'page-not-found',
  login = 'login',
}
