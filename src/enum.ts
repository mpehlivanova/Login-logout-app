export enum TOKEN_TYPE {
  bearer = 'bearer',
  access = 'access',
  refresh = 'refresh',
}

export enum PAGES {
  home = 'home',
  favorite = 'favorite',
  user = 'user',
  pageNotFound = 'page-not-found',
  login = 'login',
}
