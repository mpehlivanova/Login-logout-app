export enum TokenType {
  bearer = 'bearer',
  access = 'access',
  refresh = 'refresh',
}

export enum Pages {
  home = 'home',
  favorite = 'favorite',
  user = 'user',
  pageNotFound = 'page-not-found',
  login = 'login',
}

export enum GrantType {
  password = 'password',
  refresh_token = 'refresh_token',
}
